#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 *
 * Return: always 0 (Success)
 */

int main(void)
{
char r;

for (r = 'a'; r <= '2'; r++)
putchar(r);
putchar('\n');

return (0);
}
